# 作業ログ 2026-06-19

## 完成品保管庫

- 梅の妖精を完成品として承認しました。
- 羽や細い梅枝を保護するため、透明化せず薄背景版を本体として扱います。
- 重複した元画像は保管せず、薄背景スタンプ本体と名前入り確認画像の組み合わせに整理します。
- 完成品一覧では、アプリで使用中の妖精18体と分けて「追加完成品」として掲載します。

## 完成品一覧と手順

- `docs/stamp-catalog-2026-06-19.html` を作成しました。
- `docs/stamp-catalog-additional-fairies-2026-06-19.png` に梅の妖精を追加しました。
- 一覧の件数を「妖精19体（使用中18体＋追加1体）」へ更新しました。
- `docs/completed-design-assets-rules.md` に、完成品保管時は完成品一覧の画像版・HTML版・件数も同時に更新する手順を追加しました。

## 勲章の本番割り当て

- 参加10回のコスモス満開勲章に `medal-stage-02.png` を割り当てました。
- 先生の輪 一巡に `teacher-circle-medal-once.png`、二巡に `teacher-circle-medal-twice.png` を割り当てました。
- 先生の輪 三巡に `medal-stage-03.png`、五巡に `medal-stage-04.png` を割り当てました。
- 冒険者カードの最新勲章と、フクロウの書庫の勲章棚へ画像を表示するようにしました。
- 未獲得時は勲章画像を表示せず、画像未決定の参加20回・30回は文字表示を維持します。
- 完成品一覧へ勲章5種類を追加しました。
- JavaScriptの文法と完成品一覧の画像参照を確認しました。
- ブラウザ連動確認は実行環境の参照先を調整し、最終的に実施できました。

## 運営専用押印

- 参加スタンプと指導碁スタンプの確定直前に、共通の運営認証画面を表示するようにしました。
- 誤ったパスコードでは保存せず、正しい認証後にその1件だけを保存します。
- 保存後は認証画面を閉じ、次の押印で再度パスコードが必要になります。
- 管理画面からの確定保存と全リセットも、実行後に自動再ロックします。
- 参加、指導碁、管理画面調整、全リセットの操作履歴を端末内へ最大50件保存します。
- 管理画面には直近10件の履歴を表示します。

## 次の冒険

- 地図上に「次の冒険」カードを追加しました。
- 参加スタンプ、先生ごとの次の妖精、先生の輪の次の勲章を比較し、残り回数が最も少ない目標を一件表示します。
- 同数の場合は先生の輪、先生ごとの妖精、参加スタンプの順で案内します。
- 先生の妖精が選ばれた場合は、カードを押すと対象の先生詳細へ移動します。

## 動作確認

- 画面連動確認: 全25項目合格。
- 管理入口確認: 全9項目合格。
- 運営専用押印、次の冒険、獲得記録の保持: 全8項目合格。
- 誤ったパスコードで保存されないこと、正しい認証後だけ9回から10回へ増えること、履歴が残ることを確認しました。
- スマホ幅430pxの確認画像と結果は `docs/priority-features-check-2026-06-19/` に保存しました。

## スタンプ減算時の冒険記録

- 方針を「獲得したものは消さない」に決定しました。
- 管理画面でスタンプを減らした場合、現在の回数は減らしつつ、獲得済みの妖精、勲章、称号、特別な仲間を保存し続けます。
- 花図鑑と冒険者カードにも獲得済みの内容を表示し続けます。
- 同じ条件を再達成した場合は、報酬IDで照合して重複保存を防ぎます。
- 明示的な全リセットだけは、従来どおり獲得記録も消去します。

## バックアップ・復元

- 運営画面へ「バックアップを保存」「バックアップから復元」を追加しました。
- スタンプ数、獲得済み報酬、操作履歴、保存日時、バックアップ形式版をJSONファイルへ保存します。
- 運営パスコード、対局記録、参加者の個人情報はバックアップへ含めません。
- 復元前に参加回数、先生記録、獲得数、保存日時、履歴件数を表示します。
- 確認語「復元」が一致した場合だけ実行し、復元前の状態も先に自動バックアップします。
- 壊れたJSONと別アプリ形式を拒否するようにしました。
- バックアップと復元の往復を含む全6項目に合格しました。
- 既存機能の再確認42項目もすべて合格しました。

## 履歴項目の整理

- 履歴を日時、操作種別、対象、変更前、変更後の項目へ整理しました。
- 押印、減算、管理調整、復元、全リセットを種類別に表示します。
- 管理画面の一括変更は、参加スタンプと先生ごとの指導碁スタンプを対象別に記録します。
- 減算時も過去履歴は変更せず、新しい修正操作として追加します。
- 復元後は、復元した履歴の末尾へ復元操作を追加します。
- 端末保存は最大50件のまま、運営画面の表示を直近10件へ広げました。
- パスコード、参加者の個人情報、不要な対局内容は履歴へ保存しません。
- 履歴機能の確認5項目と既存機能48項目に合格しました。

## 妖精案内人

- 地図の「次の冒険」カード右側へ、72pxの小さな妖精案内人を表示します。
- 妖精未獲得時は、図鑑へ登録されない専用の最初の案内役を表示します。
- 獲得後は、保存済み妖精のうち最後に獲得した一体へ交代します。
- 未獲得の次巡妖精は先に表示しません。
- 次の冒険の種類に合わせて「次の花へ行こう」「先生に会いに行こう」「輪をつなぎに行こう」「旅を振り返ろう」を表示します。
- 案内人はタップ操作を受けず、画像だけが上下4pxの範囲でゆっくり浮遊します。
- 動きを減らす端末設定では浮遊を停止します。
- 妖精案内人の確認6項目と既存機能53項目に合格しました。
- 妖精を72pxのまま維持し、吹き出しを左へ10px移動、幅84px、角丸12pxへ見た目を調整しました。

## 実地運用前の通しテスト

- スマホ幅430pxで、実際の会を想定した一続きの操作を確認しました。
- 参加9回・常石先生4回の開始状態を端末ファイルへバックアップしました。
- 運営認証後、参加10回でコスモスの妖精、常石先生5回で菖蒲の妖精を獲得しました。
- 参加と指導碁を各1回減算し、現在値だけが9回・4回へ戻ることを確認しました。
- 減算後も、コスモスと菖蒲の妖精、コスモス満開勲章、称号が冒険者カードへ残りました。
- 開始前バックアップを選択し、内容確認と確認語入力後に9回・4回の状態へ復元しました。
- 復元直前の状態が自動バックアップされ、復元操作も履歴へ残りました。
- 横方向のはみ出し、画像切れ、画面エラーはなく、下部タブは40px以上、妖精案内人は72pxでした。
- 通しテスト全8項目に合格し、記録を `docs/real-world-rehearsal-2026-06-19/` に保存しました。

## 勲章デザインの微調整

- 本番割り当て済みの勲章5種類は絵柄を変更せず、アプリ内の表示だけを調整しました。
- 冒険者カードの最新勲章を34pxから42pxへ拡大し、薄い影を追加しました。
- 獲得チップ内の勲章を22pxから28pxへ拡大しました。
- フクロウの書庫の勲章を42×48pxへ統一し、勲章棚だけ淡い金色の背景にしました。
- スマホ幅430pxで、本番画像5種類、横はみ出し、画像切れを確認し、全3項目に合格しました。
- 確認記録は `docs/medal-presentation-check-2026-06-19/` に保存しました。
- 微調整後の5種類を完成版として固定しました。
- 完成品一覧の画像版・HTML版へ「完成版・アプリ使用中」を追記しました。
- 将来の勲章は達成条件が決まった時に作り、用途未定の予備は作らない方針にしました。

## 小規模な実地試用の準備

- 実際の水曜会では、最初に1〜2人分だけ押印して使い勝手を確認する方針にしました。
- 試用前後のバックアップ、参加・指導碁押印、履歴、画面の分かりやすさを確認するチェック表を追加しました。
- 試用中は新機能を増やさず、操作の迷い、見づらさ、運営負担、参加者の反応を記録します。

## 運営試用

- 試用前後のバックアップを保存しました。
- 参加スタンプを14回から15回へ変更し、保存と履歴を確認しました。
- 先生の指導碁スタンプを1回増やし、保存と履歴を確認しました。
- すべて正常に動作しました。
- 「＋」の場所と、その後の「確定して保存」までに案内が必要だったため、実際の会でもう一度観察する改善候補として記録しました。
- 詳細は `docs/field-trial-result-2026-06-19.md` に保存しました。
