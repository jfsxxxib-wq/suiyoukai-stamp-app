# イベント台帳接続の実地通し確認結果

2026年9月4日 23:02～23:08（日本時間）。ユーザーの「Google出来ました」を受け、本人限定のApps Scriptバージョン2をブラウザで操作した。本番への接続・公開は実施していない。

## 確認結果

| 項目 | 実際の結果 |
| --- | --- |
| 管理画面から保存 | 内容確認→テスト台帳へ保存。イベントID `TEST-FLOW-20260904` が1件だけ追加された |
| 内容 | 通し確認用イベント〈架空〉、2026/09/26 13:00、確認用の会場、定員3名、先生欄空欄、受付中 |
| イベント再送 | 応答 `replayed:true`。イベントは1件のまま |
| ポスター | 非公開Drive画像を読込。元画像とSHA256一致。ページを新しく開き直しても同じ画像を読込・拡大表示できた |
| 参加者から申込 | 確認画面→確定で別ファイル「申込一覧」に `TEST-FLOW-20260904-A` / 架空の申込者A が受付済として1件保存。残席3→2 |
| 申込再送 | `accepted / replayed:true`。申込1件、残席2のまま |
| キャンセル | 申込行を削除せずキャンセル状態と取消日時を保存。残席3、イベントは再開待ち |
| 停止中の新規申込 | 架空Bを試行。`paused` 応答、Bの行は作成されなかった |
| 管理者の再開 | 受付中へ更新。非公開ポータル入口のお知らせに受付中、イベント詳細に残り3名・有効な申込ボタンを表示 |
| 終了後 | テストイベントを締切に戻した。申込ボタンは無効、イベント詳細は閲覧可能 |
| 再読込 | 新しいタブから台帳に再接続。イベント1件、キャンセル履歴1件、締切、日時・会場・定員・画像参照・画像ハッシュを保持 |
| 他の入口 | イベント締切中に、非公開ポータルのリンクから本番ポータルと花図鑑のトップページを開けた |
| 既存テスト記録 | 旧TEST-LAST-SEATのA:F、旧TEST-B/Cを前回保存のGoogle読出し結果と機械比較し、一致 |

Google Sheetsの読出しツールでも直接確認し、画面内の応答だけで判断していない。

## 最終台帳

- イベント設定3行目：`TEST-FLOW-20260904`、締切、定員3、作成2026/09/04 23:03:47、更新23:06:47。
- 申込一覧4行目：`TEST-FLOW-20260904-A`、申込23:04:34、キャンセル、取消23:05:40。
- イベント設定G:Lにはポスター画像ID・URL・先生・作成日時・更新日時・SHA256の追加列。旧イベントのセルは保持。
- 画像ID：`1d1g1ngvWSTfpQTbUcr3qADBlbKSWYsqh`。
- 画像SHA256：`5287dea56c98661124d75b27866a41472e4e33a618e94caad6eb1a194ec9a73e`。

## 確認範囲と次段階

今回合格したのは、架空イベント1件を使う本人限定のテスト接続版。既に保存した画像の参照を選ぶ方式で、管理画面から任意画像をDriveへアップロードする一般機能は含まない。管理者操作はデプロイの「自分のみ」で限定しており、一般公開時の参加者・管理者の権限分離を検証したものではない。

正式受付台帳、過去スタンプ台帳、花図鑑コード、ポータル本番コード、GitHubは変更していない。接続プログラムはテスト台帳2ファイルに固定され、登録・受付番号発行・参加スタンプ付与の呼び出しを持たない。iPhone内の保存データを今回直接検査したわけではない。

公開前には本番用接続先の分離、公開時の認証と管理者権限、実際のイベント・定員・ポスター設定、申込機能だけの停止操作について本番構成で確認する。今回の通し確認だけで本番を公開しない。

## 保存した証拠

`outputs/event-reservation-flow-test/google-initial-readback.json`（保存・受付済）

`outputs/event-reservation-flow-test/google-final-readback.json`（再開待ち・キャンセル・最終締切と全テスト行）

前回 `outputs/event-reservation-test/google-readback.json` と比較し、旧行の一致・新イベント1件・新申込1件・締切/キャンセルを検証した。ソースは `outputs/event-reservation-flow-test/`。合格版保存先は同フォルダーの `passed-v2/`。
