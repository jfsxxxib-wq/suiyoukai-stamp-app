# GitHub Pages 本番公開チェック 2026-07-05

本番URL: https://jfsxxxib-wq.github.io/suiyoukai-stamp-app/

目的: 参加者向け公開モードとして、トップ画面、ホーム画面追加、QR、管理画面の公開制御、PWA設定が8/30に向けて使える状態か確認する。

## Codex確認済み

- トップ画面のHTMLは本番URLで正常に読める。
- CSS、JavaScript、スタンプルール用JavaScriptは本番URLで正常に読める。
- PWA用の `manifest.webmanifest` は本番URLで正常に読める。
- ホーム画面用アイコン `192x192` と `512x512` は本番URLで正常に読める。
- iPhone用アイコン `180x180` は本番URLで正常に読める。
- 公開されたJavaScript内に、以前の固定パスコード `suiyoukai2026` は残っていない。
- 運営用パスコードは、運営端末の初回設定で端末内に保存する形になっている。
- `#admin` だけで管理画面を直接開く入口は止めている。
- QR反映は `?stamp=` 形式で、Safariやホーム画面アプリで消えやすい `#` に頼らない形になっている。
- 参加QR、先生ごとの固定QR、対局内容QRを作る処理は本番公開されたJavaScript内に入っている。

## PWA確認

- アプリ名: 水曜会 花スタンプ冒険記録
- ホーム画面表示名: 水曜会 花記録
- 起動先: `./`
- 表示形式: `standalone`
- テーマ色: `#f6f2e4`
- アイコン: `assets/app-icon-192.png`、`assets/app-icon-512.png`

## 実機確認すること

- iPhone Safariで本番URLを開く。確認済み。
- 共有ボタンから「ホーム画面に追加」が出る。確認済み。
- ホーム画面名が `水曜会 花記録` になる。確認済み。
- ホーム画面アイコンが花のアイコンになる。確認済み。
- ホーム画面から開いて、画面が大きく崩れない。確認済み。
- 運営端末で隠し入口から管理画面を開き、8文字以上のパスコードを初回設定できる。
- 2回目以降、同じ運営端末で設定したパスコードで管理画面を開ける。
- 本番URL上の管理画面で、受付QRを作る。
- 本番URL上の管理画面で、先生ごとの固定QRを作る。
- 本番URL上の管理画面で、対局内容QRを作る。
- それぞれのQRを本人スマホ想定の端末で読み、冒険者カードの今日の記録へ着地する。
- 対局内容QRでは、今日の記録に「今日の指導碁」カードが出る。
- 先生ごとの固定QRでは、先生スタンプだけが出る。
- 同じQRをもう一度読んでも、同じ日のスタンプが二重に増えない。

## 当日の注意

- 8/30で使うQRは、本番URLを開いた管理画面から作り直す。
- `192.168...` のQRは会場内テスト用なので、本番紙面には使わない。
- 今日の指導碁カードを確認する時は、先生ごとの固定QRではなく、先生、日付、ハンデ、勝敗を選んで作る対局内容QRを使う。
- GitHub Pagesは静的公開なので、サーバー側の本格認証ではない。
- GoogleフォームURLは公開コード内にあるため、フォーム側で「回答者が他人の回答を見られない」設定を確認する。
- 参加者には管理画面の入口や運営用パスコードを伝えない。

## 実機確認結果

確認日時: 2026-07-05 19:34-19:37頃

- iPhone Safariで本番URLを開けた。
- ホーム画面に `水曜会 花記録` として追加できた。
- ホーム画面の花アイコンから開けた。
- 冒険者カード、今日の記録、QR読み取り後の表示が崩れなかった。
- 参加スタンプも表示できた。

## 判定

本番公開の土台は合格。

ホーム画面追加も合格。

残りは、紙QRの実印刷確認と、8/30当日朝の通信確認。
