# 今後の日程の見本

最新の配置指示：悦子さんが既存「イベントのお知らせ」の入口とページを画像で指定。2件はイベントのお知らせへ掲載し、上の日程欄には定例会も載せる。以下の独立した「今後の日程」案は取り下げ、見本を既存イベントページへの掲載に更新。

悦子さん提供：2026/9/23（水）12:30 三鷹洪道場 交流戦。2026/9/26（土）13:00 囲碁サロン湘南 常石隆志六段 指導碁＆定例会。「12;30」は12:30へ表記整理。曜日は2026年の日付と一致。

本番変更前の確認用候補をwork/portal-production-20260904/outputs/upcoming-preview/index.htmlに作成。既存の「今月の日程を確認」を「今後の日程を見る」に変更し、専用ビューへ移動。上下に「← ポータルへ戻る」を配置。提供された2件を日付順に表示。会場住所・料金・申込方法・終了時間は提供されていないため追加していない。既存ホームの次回9/9表示は変更なし。

ローカル見本 http://127.0.0.1:4195/ 。本番のportal.html、登録プログラム、花図鑑、台帳、スタンプデータは未変更。見本のAPIは未登録状態を返すローカル専用スタブで、Googleや本番受付には接続しない。

検証：実ブラウザでポータル→日程→ポータルの往復を確認。2件の氏名・会場・日時を画面確認。全インラインスクリプト構文チェック。実際のクリックハンドラーを隔離実行して3往復し、保存領域・通信を使わないことを確認。既存の登録クライアントとサーバーはそのまま。

AGENTSの見本確認の約束に従い、承認待ち。本番公開はまだ行わない。

## イベント掲載へ修正

build-events-preview.mjsで本番portal.htmlを基に候補を再作成。上の日程・次回定例会欄と全scriptタグは本番と完全一致。既存イベントページに2つのdetailsカードを追加し、日時・会場・先生を記載。開催日順。入口の案内文に「9/23 交流戦・9/26 指導碁＆定例会」を表示。空表示を隠し、既存の非表示編集用テンプレートは保持。申し込みや共有の新機能は追加していない。独立したupcoming-scheduleビューは除去。

verify-events.mjsで配置、2件の確定情報、上の日程欄不変、全script不変を確認。未公開、修正した見本の確認待ち。
