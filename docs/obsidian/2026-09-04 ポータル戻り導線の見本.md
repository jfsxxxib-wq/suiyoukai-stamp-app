# ポータル戻り導線の見本

花図鑑の3タブの下に本番ポータルへ戻る通常リンクを置く候補を作成。番号・スタンプ保存の処理は追加しない。登録済みセッションでの番号維持、GETのみで自動再登録なしを隔離テストで確認。本番変更前に見本承認待ち。詳細は「2026-09-04 花図鑑からポータルへ戻る導線の見本.md」。

追記：悦子さんの「お願いします」で見本承認。index.htmlのボタンとCSSだけをcommit b44bd7db89e4419d854e0b1e245003ccaaaf7c21で反映。登録・番号・スタンプを変更するコードは追加していない。再検証成功。

公開完了（Pages built）。本番の戻るボタンを実ブラウザでクリックし、同じタブで本番ポータルへ移動することを確認。双方向の導線が利用可能。
