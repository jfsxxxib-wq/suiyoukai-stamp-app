# 正式受付台帳接続・承認後の実装状況

2026-09-04。悦子さんがサイドパネルの入口見本から正式な仕組みを完成させることを承認。不要な似たサイトの整理も依頼。

## 最新状況：18時台に公開完了

この節が現在の状態。後段の認証待ち・未公開の記述は作業経緯として残す。

- Googleは指定のjfsxxxib@gmail.com。正式Apps Script `17acDbLRWrQVqHu3BZ__9SG1-dz5Coq-7t3gDj_LGVA4S6rflVmQU9r39` のウェブアプリ版1を公開済み。
- 正式台帳へ試験登録を2回送って1行だけになること、既存の花記録番号を同じ行へ追記できることを実確認。試験操作ID `gate-abfe5228-7bf5-4105-b2ee-c353c9af687f` のA14:P14のみ消去。既存9件は試験前後の値を比較して変更なし。
- 初回登録日時・姓・名・参加者番号・登録状態をL:Pへ追加済み。初回登録は参加実績ではないため参加日・スタンプは付かない。
- ポータル版8を公開。URL: https://suiyoukai-portal.c84s4n967v.chatgpt.site 。ソース `ce816217fe65238edd623f914b37fc6ff6971b0a`、保存版 `appgprj_6a98c03745948191aa074d66068c7348~appgver_8665896540388191a00ea9cf239a1b4a`、配備 `appgdep_6a9a92def3388191b0653a096e9124b4`、環境revision2、succeeded確認済み。
- Google保存応答の参加者番号と版番号を照合し、単なるサービス稼働応答では登録完了にしない修正を含む。
- 本番D1にgate_participants、gate_tickets、gate_devices、gate_ratesの4表があることを確認。ブラウザで本番の初回姓名フォーム表示を確認。
- 花図鑑GitHub mainへ `ed6dfb3774b3788146d597cb98d22372aa6d6253` を保存。Pages builtと実ページを確認。花図鑑「サイトの登録を確認する」から同じタブで本番入口へ遷移することも確認。
- QR PNG/SVG: `outputs/production-qr-20260904/suiyoukai-entrance-qr.*`。PNGを別ライブラリZXingで読み取り、上記入口URLと完全一致を確認。
- 検証範囲：Googleへの実保存は接続処理への直接送信、登録・番号引継ぎ・既存スタンプ維持は模擬環境、本番ブラウザはフォーム表示と入口への導線を確認。実参加者の本番フォーム送信から花図鑑までの通し確認はまだ。iPhone Safariの実データ確認・消えたスタンプの復元は未完了。
- 整理：空Apps Script1件を復元可能なゴミ箱へ移動、ダミー画面6件を本番入口から除去。Sitesの非公開試験2件は削除操作が利用できず残る。実記録のある9月2日サイトは保持。別Googleアカウントの未配備スクリプトは未削除。
- 未完了：8/30・9/2の参加・指導碁・スタンプの過去反映、二枚目の実績台帳、先生本人認証。今回の初回登録公開とは別工程。

## 実装した変更

- ポータル用作業場所: `work/portal-production-20260904`。既存の本番ポータルのソースから取得。
- 花図鑑用作業場所: `work/flower-production-20260904`。GitHubの現行公開ソースから取得。9月3日の既存連携処理も保つ。
- 姓・名をサイトで登録し、固定8桁の参加者番号と初回登録日時を保存。台帳の保存確認後だけ完了・花図鑑ボタンを有効にする。
- 正式台帳の既存A:K・9件を残す。L:Pに初回登録日時・姓・名・参加者番号・登録状態を加える設計。新規の初回登録は参加日を自動記入しない。
- 15分の連携券で、花図鑑の既存8桁番号と登録を結ぶ。登録だけでスタンプは付けない。
- 花図鑑の既存スタンプ・指導碁保存キーを変更しない。受け取った登録情報は別のキーに保存。
- ポータルの古いダミー連携5画面とダミー花図鑑1画面、対応する見本用スクリプトを公開候補から除去。

## 検証済み

### 18:25以降の実確認

- Google権限は通っており、setupLedgerの再実行が18:25:14に正常完了した。ユーザーに許可画面を再度探す必要はないと伝達。
- SheetsコネクターでA4:P14を読み、L:Pの5見出しと既存9件の維持を確認済み。これ以前の「列追加未実行」は更新済み。
- Googleウェブアプリの初回配備は再読込を求めるエラー。管理画面で配備がないことを確認して再試行したところ、「このウェブアプリケーションを使用するには、データへのアクセスを許可する必要があります」「アクセスを承認」で停止。実行ユーザーjfsxxxib、アクセス全員（コード内の秘密照合が必須）。同じ権限へのユーザー承認は既にある。別窓の操作待ち。
- ポータルソースのコミット `721e68ed496261efca68108f27ca24cc7272f517` をSitesへpush済み。検証済み配備用アーカイブを保存したバージョンは7。保存ID `appgprj_6a98c03745948191aa074d66068c7348~appgver_1e0a26438060819198a004b788781e6d`。まだ配備していない。公開版は従来版のまま。
- `tools/verify-ledger.mjs` を準備。URL・秘密値を標準入力のJSON1行で渡し、正式台帳に「接続確認 試験専用」を同一操作IDで2回送信＋番号連携更新する試験。実行後はK列の操作IDを正確に照合し、その新規試験行だけを消す。まだ実行していない。

- 登録番号の維持、重複再送、同名別人の分離、台帳通信停止からの再確認。
- 他サイトからの登録拒否、本人の花図鑑への引継ぎ、連携券の使い回し拒否・期限切れ拒否・同じ端末の再試行。
- 花図鑑の既存スタンプ・指導碁記録が接続前後で変わらず、花図鑑タブが開くこと（模擬環境）。
- ポータルの本番向けビルド成功。Drizzleの初回マイグレーションを生成。

## Google側の停止点

- 新規専用Apps Script: 水曜会 正式受付台帳 サイト登録受信。
- 編集URL: https://script.google.com/home/projects/1CopyS8YU3rZQZ8d6Q3fhiaZGLsBC1-IZmesZgAfC5FgXQ1wkJ9enPkHF/edit
- えつごろう（gorouetsu58@gmail.com。正式台帳の編集者）のアカウントで作成し、`google-apps-script/Code.gs`を保存済み。
- `setupLedger`実行時にGoogleの初回権限確認。「権限を確認」を押してもCodex内ブラウザには認証画面が開かず、実行待ちをキャンセル。
- 実際の台帳への列追加・登録書き込みは未実行。既存9件には変更なし。
- 悦子さんに普段のブラウザで上記ページを同アカウントで開けるか質問済み。新しい権限をこちらで付与する前は、実際の権限範囲を確認する。
- 公開ポータルのSites設定にLEDGER_SECRETを秘密値として保存（環境リビジョン1）。ソースに秘密値は保存しない。Apps Scriptには照合用ハッシュのみ。
- LEDGER_URL未設定。正式台帳の実書き込み確認、本番公開、QR再作成は未完了。

## サイト整理

追加の空Apps Script `1wCB_-tjQL7rv8XVAcenJeSK4Y4pJRPw7G_fBpDqnXS781WFfSpwN-F9s` は初期の空myFunctionだけであることを確認後、ゴミ箱へ移動済み。画面の「ファイルを削除しました。ドライブの[ゴミ箱]フォルダから復元できます」と正式設定1個だけの一覧を確認。これに対応する余分なタブも閉じた。

|対象|判断|
|---|---|
|水曜会ポータル|承認された本番入口として継続|
|水曜会 受付・9月2日未公開見本|名前は未公開見本だが実際は公開済みで9月2日の実記録6件がある。削除しない|
|水曜会 花記録・非公開テスト受付|8月31日の初期ダミー受付。整理候補|
|水曜会 参加者番号連携 非公開テスト|9月3日の接続試験用。本番確認後の整理候補|

Sites連携ツールにはサイト削除操作がない。上記2サイトはまだ削除していない。名前変更で削除済みとは扱わない。無関係なサイトは対象外。

## 続き

### アカウント方針の訂正

悦子さんがGoogleの同意画面の画像を提示。権限範囲がjfsxxxibアカウントの全スプレッドシートの参照・編集・作成・削除であることを明示し、今回作成の「水曜会 正式受付台帳 サイト登録受信」にその権限を許可することについて「はい」と明確に承認済み。同じ範囲で再確認は不要。Googleの同意画面は操作可能タブ一覧に出ない別ウィンドウのため、最終の許可ボタンは悦子さんに押していただく。

9/4 18時台: 悦子さんがCodex内ブラウザにjfsxxxibでログイン完了。Apps Scriptの新規作成が成功し、正式アカウントの専用設定へ準備済みソースを保存した。新しい編集URLは https://script.google.com/home/projects/17acDbLRWrQVqHu3BZ__9SG1-dz5Coq-7t3gDj_LGVA4S6rflVmQU9r39/edit 。以降はこちらだけを本番接続に使用する。

同アカウントでも、Codex内ブラウザの「権限を確認」を自動操作で押すと、ダイアログは閉じるが認証画面が開かず待機する。通常Edgeは複数ログインのauthuser=1で同じ404。別の自動クリック方法でも改善しなかったので、初回権限確認ダイアログを表示して悦子さんの直接操作を待つ。許可付与、setupLedgerの完了、本番公開はまだ実行できていない。

追加の未使用プロジェクト `1wCB_-tjQL7rv8XVAcenJeSK4Y4pJRPw7G_fBpDqnXS781WFfSpwN-F9s` が「Apps Scriptを作成」ボタンから遅れて別タブに開いた。タイトルは「無題のプロジェクト」。内容を未確認のため、整理時に空であることを確認してゴミ箱へ移動する候補。正式接続には使わない。

悦子さんから、通常利用の **jfsxxxib@gmail.com** で進めるよう指定。正式台帳の所有者と接続処理をこのアカウントへ統一する。先にgorouetsu58で作った未配備のスクリプトは本番に使わず、jfsxxxibで接続処理を用意できた後の整理対象とする。

Edgeではjfsxxxibとして正式台帳を開けるが、「Apps Script」へ進むとGoogleの複数アカウント認証URLで404になる。Google公式もApps Scriptの複数同時ログインは非対応と案内しているため、Codex内Googleのgorouetsu58をログアウトし、jfsxxxibの単独ログインへ切替開始。通常のEdgeログインは変更していない。

1. Googleの権限確認を完了し、setupLedgerを実行。正式台帳の既存9件が変わらないことを確認。
2. 専用Apps Scriptをウェブアプリとして配備し、LEDGER_URL設定、認証付きの実保存確認を行う。
3. 修正版のポータルと花図鑑を同時に使える順番で公開。入口が保存できない段階で花図鑑の参加ボタンだけ先行公開しない。
4. 公開URLで通し確認と本番QR作成。既存端末の実スタンプの有無は手元端末で確認が必要。
5. 削除UIが利用できる場合に不要サイトを整理。8/30・9/2の過去記録反映と先生本人認証は別工程で未完了。
