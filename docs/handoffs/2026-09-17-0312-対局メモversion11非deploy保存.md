# 対局メモ・リンク修正 version 11非deploy保存

記録日時：2026-09-17 03:12（日本時間）  
対象作業：対局メモ入り正式リーグsourceの2リンク修正をSites sourceへ保存し、新versionを未deployで保存。

## 承認範囲

- 悦子さんから、Sites sourceへの保存と新versionの非公開保存を進める明確な承認を受けた。
- Sites専用sourceのcommit・push、新versionの1回保存、保存内容の読み取り確認まで。
- deploy、公開切替、本番D1、正式スプレッドシート、Apps Script、親GitHubは変更しない。

## 開始確認

- 親Gitは記録値と一致。
- 正式リーグsourceはbranch `main`、HEAD／`origin/main` `9052139e0f6fea43360259ecab3f2533a0a9e33b`から開始。
- 未保存差分は承認済みの`app/admin/page.tsx`、`components/admin-console.tsx`だけ。
- `git diff --check`合格。未stageの別差分なし。
- Sitesはactive、public、最新保存version 10。公開URLあり。
- version 10は対局メモ入り、version 9はrollback先。新version保存前の公開URLは記録済み。

## Sites source保存

- 上記2ファイルだけをcommit。
- commit：`5479b578fb182fe481e43e03960982bd9239e23e`
- 親commit：`9052139e0f6fea43360259ecab3f2533a0a9e33b`
- commit差分は2ファイル、2挿入、4削除だけ。
- Sites専用source `main`へpush成功。
- 保存後、HEADと`origin/main`は上記commitで一致、divergence 0/0、worktree clean。
- 自動publish-on-pushは要求せず、受理状態もfalse。pushによる公開なし。

## archive作成・検査

- production buildは直前工程で合格済み。sourceはその後変更なし。
- Windowsでは通常PATHに`bash`がないため、検査済み既存手順で作業用hard linkを一時作成し、Sites 0.1.70公式packagerを実行。
- 公式scriptとBash相当実体のSHA-256は固定記録と一致。
- 公式packagerはexit 0。
- archive：`tmp/sites-version-11-20260917-0300/league-v11-5479b578.tar.gz`
- ローカルarchive：500,668 bytes、SHA-256 `6a7272c98c46fdbb7a9e406cee42411b192ffe81a89a9385708533a02eef110a`。
- archive内通常ファイル109件。build output 109件と欠落0、余分0、hash不一致0。
- Worker entry、hosting、migration 2件を確認。
- `node_modules`、`.git`、`.env`、DB実体、試験ファイルなどの禁止物0。
- 絶対path、`..` path、symlink・special entry 0。
- 保存成功後、作業用runtimeと展開検査先を削除。検証済みarchiveだけを保持。

## Sites version保存結果

- 新しい保存version：**11**。
- source commit：`5479b578fb182fe481e43e03960982bd9239e23e`。
- 保存archive：109ファイル。
- Sites側content hash：`sha256:b393ba4e23d8f0900b8bb1dbd2634a04c2b031606c38ba681105ee966a2c80a5`。
- version 11のdeployment ID：なし。
- version 11は未deployで、公開対象ではない。
- Siteのlatest saved versionは11へ更新。
- 公開URLは保存前後で一致。アクセス範囲はpublicのまま変更なし。
- 公開中の水曜会リーグはversion 9のまま。

## Git・公開・本番

- 親branch：`codex/checkpoint-2026-09-04-passed`。
- 親HEAD：`d3b278609d3f11e2ed2fad33e99bff1467c01f3d`。
- 親`origin/main`：`82013b5f8a1e5974601a6c212f34065c8de60bcd`。
- 親worktree：従来変更を含む566 status行。親GitHub commit・pushなし。
- 正式リーグsource：branch `main`、HEAD／`origin/main` `5479b578fb182fe481e43e03960982bd9239e23e`、clean。
- 公開：変更なし。リーグversion 9、ポータルversion 19。
- 正式D1、本番データ、migration、正式スプレッドシート、Apps Script：書き込みなし。

## 次回の安全な再開地点

- version 11のsource commit、archive、deployment IDなし、公開URL不変を再読取する。
- deployする場合は、version 11だけを対象に、公開後確認項目とversion 9 rollback条件を示して悦子さんの明確な承認を得る。
- 保存済みversion 10は再deployしない。
- 公開後も対局メモ、2リンク、管理者認証、正式対局の非書き込み確認を行い、異常時は新規修正をせずversion 9へrollbackする。

## 触らない

- 悦子さんの明確な承認前のversion 11 deploy、公開切替、version 10再deploy。
- 正式D1、本番データ、migration、正式スプレッドシート、Apps Script。
- 画面確認専用routeの正式source混入。
- 期限切れ復旧ticketの再利用、本人不在での新QR発行。
- 親リポジトリのGitHub commit・push、PR、merge、main。
- 秘密値、PIN、pepper、token、実人物の行本文。
