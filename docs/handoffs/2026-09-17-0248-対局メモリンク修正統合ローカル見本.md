# 対局メモ・リンク修正統合ローカル見本

記録日時：2026-09-17 02:48（日本時間）  
対象作業：未公開の管理画面コピー専用対局メモと、管理画面内2リンク修正を合わせたローカル見本。

## 開始ゲートと再開地点

- 悦子さんから、再開対象は「管理画面で対局メモを作り、Codex君へ渡す文章をコピーする機能」と確認した。
- 対局メモは承認済みversion 10で公開確認まで合格したが、既存リンク非遷移を検知してversion 9へrollbackしたため、現在の公開版には未反映。
- その後、リンク非遷移はvinext環境の`next/link`処理が原因候補と切り分け済み。
- 最新CURRENTの荒川光平さん修復工程は完了済みで、次回は本人同席時まで待つ状態。今回の作業では触れていない。

## 作成したローカル見本

- 保存先：`work/league-v10-memo-native-anchor-preview-20260917/`
- 基準：Sites version 10と一致する正式候補source commit `9052139e0f6fea43360259ecab3f2533a0a9e33b`。
- 基準sourceの追跡72ファイルを欠落なく複製。
- 対局メモ機能、表示、文言、CSS、D1 schema、migration、API、認証、正式対局保存処理は変更していない。
- 正式反映候補の差分は次の2ファイルだけ。
  - `app/admin/page.tsx`：端末復旧リンクを`next/link`から通常の`<a href>`へ置換。
  - `components/admin-console.tsx`：参加者画面リンクを`next/link`から通常の`<a href>`へ置換。
- 見た目、文言、リンク先URLは変更なし。
- 画面確認専用に`app/preview/admin-match-copy-memo/page.tsx`をローカル見本へ追加。架空3名だけを使い、正式反映対象には含めない。

## 試験と画面確認

- 既存Node試験：11/11合格。
- production build：合格。
- 基準sourceとのhash照合：欠落0、相違は上記2リンクファイルだけ。画面確認専用routeは別管理。
- ローカル画面で「Codex君へ送る対局メモ」「正式保存はしません」「コピー専用」を表示。
- 架空の勝者・敗者・場所を使って1件追加し、番号付きコピー内容を表示。
- 「1件をまとめてコピー」を押し、「コピーしました。Codex君へのメッセージに貼り付けてください。」を確認。
- 「この欄から正式対局へは登録されません。」を表示。
- ローカル見本タブは悦子さんが右側で確認できる状態にした。

## Git・公開・本番

- 親branch：`codex/checkpoint-2026-09-04-passed`。
- 親HEAD：`d3b278609d3f11e2ed2fad33e99bff1467c01f3d`。
- 親`origin/main`：`82013b5f8a1e5974601a6c212f34065c8de60bcd`。
- 親worktree：従来変更を含む564 status行。今回のローカル見本と記録以外は変更していない。
- 正式リーグsource：branch `main`、HEAD／`origin/main` `9052139e0f6fea43360259ecab3f2533a0a9e33b`、clean。
- GitHub commit・push：なし。
- 公開：変更なし。リーグversion 9、ポータルversion 19。
- Sites version 10：保存済み、現在は非公開。
- 正式D1、本番データ、正式スプレッドシート、Apps Script：今回の書き込みなし。

## 次回の安全な再開地点

- 悦子さんが右側の対局メモ見本を確認する。
- 明確な承認があるまで、正式リーグsource、Sites version保存、deployへ進まない。
- 承認後は、画面確認専用routeを除外し、正式候補sourceへ2リンク差分だけを反映する。
- 正式反映後に試験・build・差分確認を行い、新version保存とdeployは範囲を分けて確認する。

## 触らない

- 明確な承認前の正式source反映、新Sites version保存、deploy、version 10再deploy。
- 正式D1、本番データ、migration、正式スプレッドシート、Apps Script。
- 荒川さんの期限切れticket再利用、本人不在での復旧QR再発行。
- 既存実績、正式台帳行、参加者行、スタンプの削除・変更。
- 親リポジトリのGitHub commit・push、PR、merge、main。
- 秘密値、PIN、pepper、token、実人物の行本文。
