# 登録修復機能 HTTPS Git再停止記録

記録日時：2026-09-16 20:54（日本時間）

## 再開時の承認と確認

- 悦子さんから「pushから再開」と、前回と同じ一括承認条件を受領。
- push・deploy・公開・GitHub pushは自動再試行しない条件を継続。
- `docs/CURRENT.md`、前回handoff、親Git、正式ポータルSites sourceを照合し、記録値との一致を確認。
- 水曜会ポータルはversion 15公開中、本番D1は既存11表のみで、修復用6表がまだ存在しないことを読み取り確認。

## 今回行ったこと

- GitHub Desktop同梱Git `2.53.0.windows.4` を検出。
- 同梱ディレクトリ内に `git-remote-https.exe` 実体があることを確認。
- 新しいSites短期資格情報を取得し、ファイルへ保存せず、per-command認証で正式sourceのpushを1回だけ実行。
- 実行前に正式sourceがcleanで、HEADが `47b7a15a213abeb0ed5c16a6b9eec2ab2a24996c` であることを再確認。

## 停止理由

- GitHub Desktop版GitもHTTPS補助を実行パスから認識できず、`git: 'remote-https' is not a git command` で通信前に終了。
- pushの自動再試行は禁止されているため、`GIT_EXEC_PATH`変更などを使った次の送信は行わず停止。
- 短期資格情報は永続化していない。

## 外部影響

- Sites source remote：変更なし。`origin/main` は `8e07a5daee7be2405be285ad7f6e8287beea3a3a` のまま。
- ポータル公開：変更なし。version 15のまま。
- version 16保存：未実施。
- deploy：未実施。
- 本番D1：変更なし。修復案件作成、使用停止、統合、番号発行なし。
- 正式台帳、Googleスプレッドシート、Apps Script：変更なし。
- 親GitHub：変更なし。

## Git照合

正式ポータルSites source：

- branch：`main`
- local HEAD：`47b7a15a213abeb0ed5c16a6b9eec2ab2a24996c`
- `origin/main`：`8e07a5daee7be2405be285ad7f6e8287beea3a3a`
- worktree：clean

親リポジトリ：

- branch：`codex/checkpoint-2026-09-04-passed`
- HEAD：`3614d8ffc028dc31b1ee7ccbda89d9979734b434`
- `origin/main`：`82013b5f8a1e5974601a6c212f34065c8de60bcd`
- 既存dirty worktreeを保持。

## 次回の安全な再開地点

1. 正式sourceのHEAD、clean状態、Sites version 15、本番D1既存11表を再照合する。
2. GitHub Desktop版Gitの `mingw64/bin` を `GIT_EXEC_PATH` に指定した非通信診断で、HTTPS補助を呼べる構成だけを確定する。
3. 悦子さんから新しい「pushを1回実行」の承認を得た後、新しい短期資格情報でpushを1回だけ行う。
4. push成功後だけ同一SHAをbuild・packageし、version 16保存、deploy、公開後確認へ進む。

## 触らない

- 新しい明確な承認なしのpush再送。
- push成功前のversion 16保存・deploy。
- 既存実績、正式台帳行、既存参加者行の削除。
- 8桁番号の手動発行。
- 実人物の修復案件作成、正本指定、使用停止、統合。
- 対象外の本番D1、正式スプレッドシート、Apps Script、秘密値。
