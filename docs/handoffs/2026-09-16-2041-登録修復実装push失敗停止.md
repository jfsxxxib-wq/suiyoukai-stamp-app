# 登録修復機能 実装完了・push失敗停止記録

記録日時：2026-09-16 20:41（日本時間）

## 承認範囲

- 要件整理、ローカル見本、実装、試験、正式ソース反映、version保存、本番前確認、deploy、公開後確認、GitHub commit・push、記録更新まで一括承認。
- 既存実績の削除、正式台帳の行削除、8桁番号の手動発行、対象外の本番データ変更、秘密値変更、戻せない操作が必要なら停止。
- push・deploy・公開は自動再試行しない。

## 実装済み

- 正式ポータルSites source：`work/portal-v15-site-source-20260914/`。
- A〜E分類の修復案件、修復案件ID、根拠資料、正本候補、記録ID照合、監査履歴を追加。
- 使用停止は、修復案件ID、操作者ハッシュ、日時、理由、根拠資料、前後状態、取消元イベントを追記保存する設計。
- 参加者番号は正式登録でシステム発行済みの8桁だけを候補へ追加。修復画面から番号発行はしない。
- 正本指定前に根拠資料を必須化。E以外では使用停止不可。正本自身は使用停止不可。
- 使用停止中の登録は、通常受付、花端末、花連携券、リーグ連携券、端末復旧から利用不可。
- 使用停止は案件再開後に監査付きで取消可能。
- 実績は削除・移動せず、安定した記録IDごとに照合待ち／確認済みを保存。
- 案件完了には正本の参加者番号・花番号の両方、Eの旧登録停止、全記録IDの照合完了が必要。
- D1 migration：`drizzle/0005_elite_george_stacy.sql`。新規6表と索引のみで、既存表の行削除・列更新なし。

## ローカル見本

- 保存APIへ接続しない `repair-preview=1` の見本を作成。
- Codex内ブラウザで全体表示を確認。
- 見本用HTTPサーバーは終了済み。

## 試験結果

- `tools/test-repair-admin.mjs`：合格。
  - 同一操作IDの冪等性、番号伏字、正本指定、記録ID照合、使用停止・再開、追記監査、参加者行非削除。
- `tools/test-gate.mjs`：合格。停止中の受付セッション・花端末拒否を含む。
- `tools/test-portal-admin.mjs`、`test-meeting-schedule.mjs`、`test-history.mjs`、`test-gate-client-recovery.mjs`：合格。
- 今回対象ファイルのoxlint：警告・エラーなし。
- build：合格。
- 全体lintは既存React/UI雛形の既知エラーで非合格。今回追加分の指摘はすべて解消済み。

## Gitと停止理由

正式ポータルSites source：

- branch：`main`
- local HEAD：`47b7a15a213abeb0ed5c16a6b9eec2ab2a24996c`
- local commit：`Add audited participant registration repair workflow`
- `origin/main`：`8e07a5daee7be2405be285ad7f6e8287beea3a3a`
- local mainはorigin/mainより2コミット先行。直前のversion 15候補コミットも未pushだった。
- Sitesの短期資格情報を使った最初のpushは、同梱Gitに`remote-https`補助コマンドが無く失敗。通信前に終了し、リモート変更なし。
- pushは自動再試行禁止のため停止。version保存、deploy、公開は未着手。

親リポジトリ：

- branch：`codex/checkpoint-2026-09-04-passed`
- HEAD：`3614d8ffc028dc31b1ee7ccbda89d9979734b434`
- `origin/main`：`82013b5f8a1e5974601a6c212f34065c8de60bcd`
- 既存dirty worktreeを保持。今回の親Git commit・pushなし。

## 外部影響

- 公開：変更なし。ポータルversion 15のまま。
- 本番D1：変更なし。公開前読み取りでは既存11表のみで、新規修復表なし。
- 正式台帳・スプレッドシート・Apps Script：変更なし。
- 8桁番号発行：なし。
- 修復案件作成・使用停止・統合：なし。
- 秘密値：変更なし。短期Git資格情報はファイルへ保存していない。

## 次回の安全な再開地点

1. 正式ポータルsourceのHEADとorigin/mainを再照合する。
2. 新しいSites短期Git資格情報を1回だけ取得する。
3. HTTPS補助コマンドを備えた別のGit実行ファイルで、`47b7a15...`を`main`へ1回だけpushする。
4. push成功後にHEADの完全SHAを再取得する。
5. 同一SHAからbuild・packageし、ポータルversion 16を保存する。
6. publicの現行audienceを維持して1回だけdeployし、状態を確認する。
7. 公開後に`/admin`、一般入口、D1新規表が空であること、既存データ非変更を確認する。
8. 親CURRENTとhandoffを更新し、親GitHubは今回ファイルだけを選択してcommit・pushする。

## 触らない

- pushを確認せずversion保存・deployしない。
- 既存実績、正式台帳行、既存参加者行を削除しない。
- 8桁番号を手動発行しない。
- 実人物の修復案件、正本指定、使用停止、統合をまだ実行しない。
- 本番D1の対象外データ、秘密値、正式スプレッドシートを変更しない。
