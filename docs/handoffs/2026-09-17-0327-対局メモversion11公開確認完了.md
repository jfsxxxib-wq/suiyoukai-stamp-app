# 対局メモ version 11 公開確認完了

記録日時：2026-09-17 03:27（日本時間）  
対象作業：保存済みSites version 11のrollback条件確認、deploy、公開後の読み取り中心確認。

## 開始ゲート

- 引き継ぎ元：`docs/handoffs/2026-09-17-0312-対局メモversion11非deploy保存.md`。
- 親Gitは記録値と一致。
- 正式リーグsourceはbranch `main`、HEAD／`origin/main` `5479b578fb182fe481e43e03960982bd9239e23e`で一致、worktree clean。
- version 11は上記commit、archive 109ファイル、未deployで保存済みと再確認。
- 公開中version 9は直前deployment `succeeded`、同じ公開URLで復帰可能と確認。
- 正式D1の公開前基準：13テーブル、177行、比較用集約hash `2cf0875be28993fc`。

## rollback条件

次のいずれかを検知した場合は、新しい修正を加えずversion 9へ戻す条件とした。

- deploy失敗、公開不能、5xx、Worker例外。
- 管理画面または管理者認証が開かない。
- 「参加者画面を開く」「端末復旧QRを発行」のどちらかが通常遷移しない。
- コピー専用対局メモが追加・コピー・再読み込み時消去の要件を満たさない、または正式D1へ保存される。
- 既存の正式対局画面、会員、端末、復旧対象の読み取り表示が壊れる。
- 正式D1の行・内容、migration、外部サービスに予期しない変更が出る。
- version 11のsource、archive、保存versionが承認済み内容と一致しない。

## deploy結果

- Sites version 11だけをproductionへdeploy。
- deploymentは`pending`、`publishing`を経て`success`。
- 公開URLは変更なし：`https://suiyoukai-league.c84s4n967v.chatgpt.site`
- 公開accessは`public`のまま。
- version 11のsource commit、archive 109ファイル、Sites側content hashは保存時記録と一致。
- version 9は保存済みrollback先として残している。

## 公開後確認

- `/admin`：管理者認証済み画面を表示。
- 「参加者画面を開く」：クリックで参加者画面へ通常遷移。
- 「端末復旧QRを発行」：クリックで`/admin/recovery`へ通常遷移。
- 対局メモ：日付、異なる勝者・敗者、場所省略で1件追加。
- コピー内容：番号付き1件が表示され、コピー完了表示を確認。
- 再読み込み後：0件へ戻り、正式保存されない表示も復帰。
- 管理画面：正式会員38名、承認済み端末2件、復旧対象3名を表示。
- 正式対局画面：入力欄、未確定・確定済み一覧を表示。正式D1書き込みを避けるため確定・訂正は実行していない。
- 正式D1：公開後も13テーブル、177行、集約hash `2cf0875be28993fc`で公開前と完全一致。
- Worker：5xx・例外なし。既知の`/favicon.ico` 404が2件のみで、outcomeは`ok`。

## 判定

- rollback条件への該当なし。
- version 9へのrollbackは実施せず、version 11を公開継続。
- 新しい修正、migration、正式D1書き込み、正式スプレッドシート、Apps Script変更は行っていない。

## Git・公開・本番

- 親branch：`codex/checkpoint-2026-09-04-passed`。
- 親HEAD：`d3b278609d3f11e2ed2fad33e99bff1467c01f3d`。
- 親`origin/main`：`82013b5f8a1e5974601a6c212f34065c8de60bcd`。
- 親worktree：従来変更と今回の記録を含む568 status行。親GitHub commit・pushなし。
- 正式リーグsource：branch `main`、HEAD／`origin/main` `5479b578fb182fe481e43e03960982bd9239e23e`、clean。
- 水曜会リーグ：version 11公開中。
- 水曜会ポータル：version 19公開中、変更なし。
- 正式D1、本番データ、正式スプレッドシート、Apps Script：変更なし。

## 次回の安全な再開地点

- version 11を通常運用し、悦子さんの実機で対局メモを使う。
- 実機で管理画面の2リンク、対局メモ、既存正式対局に異常が出た場合は、その時点の本番データを読み取り確認してversion 9 rollbackを検討する。
- 正式対局の確定・訂正は、実際に登録する対局がある時だけ通常運用として行う。

## 触らない

- 正式D1、本番データ、migration、正式スプレッドシート、Apps Scriptへの検証目的の書き込み。
- version 10の再deploy。
- 画面確認専用routeの正式source混入。
- 期限切れ復旧ticketの再利用、本人不在での新QR発行。
- 親リポジトリのGitHub commit・push、PR、merge、main。
- 秘密値、PIN、pepper、token、実人物の行本文。
