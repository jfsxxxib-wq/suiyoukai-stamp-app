# イベント申込の未公開画面見本

確認用URL: http://127.0.0.1:4196/

## 画像入り見本の追加

ユーザー提供画像（「囲碁の時間」2026年6月の表紙）をposter-sample.jpgとして元ファイルとハッシュ一致でコピー。画像そのものは加工せず、未公開見本にだけ掲載した。更新版URLは http://127.0.0.1:4196/?poster=1 。

9/26の案内とは内容が異なるため、カード内に「画像の表示確認用です。9/26の案内ポスターではありません。」と明記。イベントの日時・先生・定員へ表紙内の文字を流用していない。

縮小表示の高さは承認済み見本と同じ136〜156px、縦横比を保持して全体表示。タップで開くダイアログに画像全体を表示し、「文字を拡大する」で原寸幅904pxに切替えてスクロールできる。「全体に戻す」「閉じる」も用意した。

ブラウザの狭い表示幅で、画像の欠けがないこと、拡大・原寸切替・閉じてカードへ戻る動作を確認。縮小表示は全体像用で、小さな本文を読む際は拡大が必要。画像入り見本の最終確認はユーザー待ち。approved-v1の保存版はそのまま。本番公開・台帳・スタンプ操作はなし。

## 見本の承認

悦子さんが「すぐわかりました」と申込ボタンの見つけやすさを確認し、その後「この形で未公開見本を確定してよい」「見本確定で賛成」と明示的に承認。ポスターの表示サイズ、イベント全体の残席表示、大きな緑の申込ボタン、入口への戻り導線を承認済みとする。「先生別の予約枠とは別です。」は残す。

承認時点のindex.html・build.mjs・serve.pyを同じ見本フォルダ内のapproved-v1へコピーし、SHA-256一致を確認して保存した。見本レイアウトの承認であり、仮の残席12名・実イベントの募集条件・本番公開への承認ではない。ユーザーのコメントにある「手合い」は現見本に具体値を掲載しておらず、未確定のまま。推測して追加しない。

次はユーザー提供の実ポスター1枚を入れた見本で、縮小・拡大時の読みやすさを確認する。現時点ではポスターの添付はなく、画像は準備中。本番化には実台帳からの残席取得に加え、管理者権限・募集日時・申込だけの停止機能など、本番前確認表の未実装部分を完成させる。表示文言の差し替えだけでは公開しない。

9/26「指導碁＆定例会」を対象に、ポスター掲載位置・イベント全体の残席・申込ボタンを未公開の別画面として作成した。9/23「交流戦」の案内も下部に残した。9/26の申込表示を先に確認するための並びであり、本番の開催日順表示の変更ではない。

## 見本の仕様

- ポスターは未提供なので、掲載位置を示す仮表示。スマホでは高さ136〜156px、背景余白と拡大案内を付けた。タップすると拡大表示し、閉じて戻れる。実画像の文字の読みやすさはポスター提供後に確認する。
- イベント名・日時・会場・常石隆志六段は悦子さんの提示内容。残り12名・受付中は仮の表示で、実際の募集状況ではない。
- 残席は「イベント全体：残り12名」と明記し、先生別の予約枠とは別であることを添えた。先生別枠の申込は今回作らず、架空の先生別定員も実イベント内へ混ぜていない。
- 「申し込む」はカード上に常時表示。「詳細を見る」を開かなくても進める。
- 申込画面→内容確認→完了画面の見本→イベント一覧の導線を用意。名前の初期値は「見本 太郎」。実名の入力は不要。
- イベント一覧と入口を往復できる。入口の定例会表示は元の見本から引き継いだ。イベント以外の未確認機能はこの独立見本では無効。

## 分離と検証

作業場所: work/portal-production-20260904/outputs/event-signup-preview/。本番portal.htmlやpublicの資産は変更せず、既存資産を読み取って表示する。合格版passed-v1にも変更なし。

本番のスクリプトは含めず、画面内の移動だけのスクリプトに置き換えた。fetch・ブラウザ保存領域・cookie操作は使っていない。サーバーはGETで見本と画像・CSSを返すだけで、POST受付はない。CSPにもconnect-src 'none'、form-action 'none'を指定した。

ビルド時にスクリプトの構文と本番スクリプト混入がないことを確認。見本ページ・既存CSSはHTTP 200で取得できた。画面サイズ・ボタンの見つけやすさは悦子さんの見本確認対象。実ポスターの寸法確認は未実施。

本番公開・GitHub保存・Google台帳操作・スタンプ付与・受付番号発行は行っていない。AGENTS.mdの約束に従い、画面への承認後に本体への組込みを進める。
