# イベント予約のテスト準備

2026年9月4日の準備時点の記録。**22:00更新：悦子さんの認証完了後、実環境で全試験を完了した。現在は認証待ちではない。最終結果は「2026-09-04 イベント予約の実地テスト結果.md」を参照。以下は実行前の経緯として保存する。**

## 作成済み

- [水曜会 テスト専用 イベント設定](https://docs.google.com/spreadsheets/d/1TMuW2zrRlvrc43ia8Au8zwmKHr2pnHZ0o6zkuURMJUU/edit)
- [水曜会 テスト専用 申込一覧](https://docs.google.com/spreadsheets/d/1Pi8EDp19YysJRNpDBNBTD8nkjwSBDIjGjBuyl8OCNW4/edit)
- [水曜会 テスト専用 最後の1枠（Apps Script）](https://script.google.com/home/projects/11ZvJAZU-SDSyAR9cd7szXvvIBJhEcQsBYyOqS7kXPhSF4fg90iesg1Yx/edit)

2ファイルのGoogleネイティブ形式・シート名をメタデータで確認。ローカルの全シートを描画確認し、Google上の表示も確認。架空イベントTEST-LAST-SEAT、定員1名のみ。申込一覧は見出しだけ。実参加者の情報は使っていない。

Google Drive取込ツールは source_file.mime_type 欠落のスキーマエラーで失敗。前回承認されたブラウザ取込で作成した。元の正式受付台帳・参加指導碁スタンプ台帳・本番サイト・花記録・GitHubは変更していない。

## 準備した試験

試験コード: outputs/event-reservation-test/Code.gs。構文チェック合格。Googleエディタへ保存済み。ウェブアプリは実行ユーザーを本人、アクセス可能ユーザーを「自分のみ」に設定。デプロイはアクセス承認待ちで、URL発行・試験実行はまだ完了していない。

1. 架空A/Bの申込みをGoogleの別実行へ並行送信。受付1件・満員1件を確認する。
2. 各実行の開始・ロック取得・完了時刻で、実行の重なりと保存区間の直列化を確認する。
3. 同じ申込IDの再送で1行・有効1件のままかを確認する。
4. キャンセル行を残し、再開待ちに変更。空席でも架空Cの新規申込を拒否する。
5. 管理者再開操作後にCを受付。最終状態は取消1行・受付済1行。テスト受付を締切にする。

確認と保存は同じApps ScriptのScriptLock内で行い、flush後にロックを解放する。[Google公式LockService](https://developers.google.com/apps-script/reference/lock/lock-service)に沿った構成。キャンセル時は先に募集を止めてから取消を記録する。異なるスクリプトや台帳の直接編集は同じロックに従わないため、募集を公開する際は保存経路を統一する必要がある。

最初のprepare処理で両ファイルの時刻設定をAsia/Tokyoに変更する。現在は取込既定のAmerica/Los_Angelesであり、prepareも未実行。イベントの日付セルは表示上2026/09/26 13:00。実際の申込日時はまだない。

## 次の再開手順

1. 専用プロジェクトのGoogle認証を完了する（テスト名とjfsxxxib@gmail.comを確認）。本番の受付プログラムは開かない。
2. 本人限定のデプロイ完了後、発行されたウェブアプリURLを開く。
3. 「試験を1回実行する」を1回だけ押す。再実行で既存テスト履歴を消さない仕様。
4. 画面の結果と時刻証拠を保存し、Google SheetsのA1:F2およびA1:G4を別途読み戻す。
5. 成功・失敗を区別して報告する。認証待ちや構文チェックをもって「最後の1枠テスト成功」と報告しない。

このコードは本人限定の検証器。一般公開用の本人確認・管理者権限・実イベントの募集日時判定・別端末の重複防止はまだ実装していない。
