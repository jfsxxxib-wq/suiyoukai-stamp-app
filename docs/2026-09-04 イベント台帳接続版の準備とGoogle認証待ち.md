# イベント台帳接続版（非公開）

**更新：23:08までにGoogle認証後の実地通し確認を完了。以下の認証待ちは経過記録。最新結果は「2026-09-04 イベント台帳接続の実地通し確認結果.md」を参照。**

2026-09-04。見本確定後、ユーザーが希望した「テスト台帳1件保存→読み戻し→申込→キャンセル→手動再開→ポータル表示」の接続準備。

## 現在の状態

- テスト専用 Apps Script のバージョン2を22:59に更新。実行者は jfsxxxib@gmail.com、アクセスは「自分のみ」を維持。
- 非公開画面の表示までは確認。最初のGoogle RPCで `Authorization required` が表示され、`Review permissions` を開いた。認可の別ウィンドウはブラウザ操作ツールのタブ一覧に出ない。
- プロジェクト概要で要求スコープを確認済み：`spreadsheets`、`script.container.ui`、`drive.readonly`。追加のDrive権限はファイルの閲覧・ダウンロードであり、Driveファイルの編集・削除権限ではない。Googleの権限表示自体は全Driveファイルを対象とするが、テストコードの画像参照先は上記1ファイルに固定。
- **今回のイベント行・申込行の実保存、Google上の読み戻し・取消・再開は未確認。成功扱いにしない。**
- 既存テスト台帳はまだ TEST-LAST-SEAT / TEST-B / TEST-C のみ。前回の結果と一致。
- 本番ポータル・花図鑑・正式受付台帳・過去スタンプ台帳・GitHubは変更していない。

## 接続対象

- イベント設定：`1TMuW2zrRlvrc43ia8Au8zwmKHr2pnHZ0o6zkuURMJUU` / イベント設定
- 申込一覧：`1Pi8EDp19YysJRNpDBNBTD8nkjwSBDIjGjBuyl8OCNW4` / 申込一覧
- テスト Apps Script：`11ZvJAZU-SDSyAR9cd7szXvvIBJhEcQsBYyOqS7kXPhSF4fg90iesg1Yx`
- 接続URL：https://script.google.com/macros/s/AKfycbzeLSZLSXIPLNBsMOPwb1hE6bKaEScPUAesP5rK0eGUW0EbyldW7MBxikO9Lw-DXkP9QQ/exec
- イベントID：`TEST-FLOW-20260904`、架空イベント、2026/09/26 13:00、確認用の会場、定員3名。
- 予約ID：`TEST-FLOW-20260904-A`、架空の申込者A。

## ポスター

ユーザー提供の「囲碁の時間」の画像を、そのままテスト専用ファイルとしてGoogle Driveに保存した。

- ファイルID：`1d1g1ngvWSTfpQTbUcr3qADBlbKSWYsqh`
- 共有メタデータ：shared=false、owner=jfsxxxib@gmail.com のみ。
- SHA256：`5287dea56c98661124d75b27866a41472e4e33a618e94caad6eb1a194ec9a73e`
- DriveAppで非公開画像を読み、画像ハッシュを確認してからイベント台帳に参照を保存する。画像読込の新しいGoogle認証が必要。
- 今回は保存済みテスト画像の参照を使用。管理画面から任意画像をGoogleへアップロードする一般機能は未実装。

## 実装と手元の検証

`outputs/event-reservation-flow-test/` に Flow.gs、ui.html、build.cjs、verify.cjs、生成Code.gs。

- 既存の合格版 `outputs/event-reservation-test/passed-v1/` は保持。
- 管理画面→保存影響確認→テスト台帳保存→イベント表示→申込確認を google.script.run で接続。
- 2台帳の固定IDと名前、列見出しを検査。架空イベント1件のみ許可。
- ScriptLock取得後に最新行を読み直して保存。イベント・予約それぞれ再送時に行を増やさない。
- キャンセルは受付を「再開待ち」にしてから取消状態と日時を記録。削除なし。再開は管理操作のみ。
- 停止後のイベント再送で再開しない。キャンセル済み予約の再送で復活しない。
- ポータル・花図鑑へのリンクは受付状態に依存しない通常リンク。登録情報・番号・スタンプの書き換え処理は含まない。
- Nodeによる構文確認と台帳を模した検証を実施。再送、内容衝突、日時、画像ハッシュ、取消履歴、停止中受付拒否、手動再開、再送後も締切保持、旧テスト行保持は合格。これはGoogle実地確認の代替ではない。

## 認可後に続けること

1. 非公開接続URLを開き、未登録の読み戻しと画像表示を確認。
2. 管理画面で募集内容確認→テスト台帳保存。イベント行1件、画像・日時・会場・定員・状態をGoogle側から読み戻す。
3. 同じ保存操作の再送でもイベント1件を確認。
4. 参加者画面で架空申込1件。再送しても予約1件。
5. 管理画面でキャンセル。履歴を残し、再開待ちでBの新規受付が拒否されることを確認。
6. 管理者再開→ポータルの受付中表示。最後に締切に戻す。
7. 停止中でもポータル・花図鑑のリンクを開けること、旧テスト行が不変なことを確認。実測結果を保存。

X・LINEの告知文生成は今後の候補。本番一般参加者の認証・管理者権限設計や一般画像アップロードを完成扱いにしない。
