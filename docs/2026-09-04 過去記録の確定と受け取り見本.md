# 過去記録の確定と受け取り見本

追記：見本とブラウザ取り込みは承認済み。実装・公開完了。以下は承認前の経緯。最新結果は「2026-09-04 過去記録受け取りの公開結果.md」を参照。

## 確定内容

悦子さんが「はい、あっています」と回答。指導碁5局は全て2026/8/30として確定。前の照合案に従い参加2回の方は8/30・9/2各1回、参加1回の方は正式台帳の参加日とした。

- 8/30：参加6名、指導碁5局（結城聡九段2局、松本武久八段3局）。
- 9/2：参加9名。
- 合計10名、参加15個、指導碁5個、種類別に分けた記録20件。
- `outputs/recovery-20260904/確定記録.json` に確定内容と安定した記録IDを保存。元の申告案は履歴として保持。

## 台帳

スプレッドシートスキルを使い `outputs/recovery-20260904/水曜会 参加・指導碁・スタンプ台帳.xlsx` を作成。参加者別集計と記録明細の2タブ。日付は型付き、番号は文字列、集計はSUMIFS、合計15/5/2/3を検証し、全2タブの描画を確認。端末への反映状況はすべて「未反映」。過去の勝敗・手合いは入力していない。

Google連携のプロフィールはjfsxxxib@gmail.com。既存の同名台帳がないことを検索で確認してからnative_google_sheetsで取り込みを試行したが、source_file.mime_type必須というコネクター入力検証エラー。ASCIIファイル名でも同じ。Google上の新規台帳作成は未完了で、成功URLはない。Google Sheetsスキルの規定ではブラウザ等の別作成経路はユーザーが明示的に許可する必要がある。

## アプリへの反映

既存の参加QR処理は当日以外を拒否する。日付制限を緩めたり、今日のQRを使ったりせず、専用の過去記録取り込みを追加する案。

`outputs/recovery-20260904/preview.html` を見本として作成し、http://127.0.0.1:4192/preview.html で表示。10名を切替可。参加の現在値を変えて不足分を表示する模擬動作と、受け取り後画面を確認。実データ保存・API通信はない。

AGENTS.mdの事前見本確認の約束に従い、この新しい受け取り画面の承認前はアプリ本体・GitHub・本番公開を変更しない。既存の入口接続は安定版のまま。

承認後に必要な実装：本人に渡す個別の受け取り方法、端末番号の対応確認、既存記録との照合と重複防止、日付別記録と累計の整合、端末の受領結果を台帳へ記録。申告合計を単純加算しない。名前一覧や個人別記録を公開ソースへ埋め込まない。番号未連携の6名は、名前だけを使って他人の端末へ自動配信しない。
