# 2026年9月2日 受付入口の安全保存

このフォルダーは、公開中の「アプリ読み取りQR入口／受付入口」について、2026年9月1日時点の承認待ち状態を記録したものです。

保存内容は次の3区分です。

1. `site/` と `SOURCE_SNAPSHOT.md`: 受付入口のソース、D1定義、Apps Script本番用ソース
2. `tests/` と `records/`: 最終試験と公開状態の記録
3. `artifacts/`: A4印刷用QR PDFとSHA-256チェックサム

この保存では `main` へのマージ、Sitesの更新、D1の書き込み、Apps Scriptの変更、QRの差し替え、Google受付帳や本番データの変更を行っていません。

秘密情報、サービス内部ID、参加者データは含めていません。設定を復元するときは、保存対象外の実値を管理者が安全な場所から設定してください。
